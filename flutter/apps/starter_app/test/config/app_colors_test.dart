import 'package:flutter_test/flutter_test.dart';
import 'package:starter_app/config/app_colors.dart';

void main() {
  group('app_colors', () {
    test('placeholder — replace with real assertions', () {
      expect(true, isTrue);
    });
  });
}
