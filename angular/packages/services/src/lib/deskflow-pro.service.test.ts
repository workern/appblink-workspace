import * as assert from 'assert';
import * as deskflow-pro.serviceModule from './deskflow-pro.service';

describe('deskflow-pro.service', () => {
  // TODO: Replace with meaningful assertions
  it('should be importable without errors', () => {
    assert.ok(deskflow-pro.serviceModule);
  });
});
