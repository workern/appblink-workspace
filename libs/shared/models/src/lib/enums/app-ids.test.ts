import * as assert from 'assert';
import * as app-idsModule from './app-ids';

describe('app-ids', () => {
  // TODO: Replace with meaningful assertions
  it('should be importable without errors', () => {
    assert.ok(app-idsModule);
  });
});
