import 'package:flutter_test/flutter_test.dart';
import 'package:starter_app/config/app_theme.dart';

void main() {
  group('app_theme', () {
    test('placeholder — replace with real assertions', () {
      expect(true, isTrue);
    });
  });
}
