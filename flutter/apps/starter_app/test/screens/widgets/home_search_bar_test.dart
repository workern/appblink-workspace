import 'package:flutter_test/flutter_test.dart';
import 'package:starter_app/screens/widgets/home_search_bar.dart';

void main() {
  group('home_search_bar', () {
    test('placeholder — replace with real assertions', () {
      expect(true, isTrue);
    });
  });
}
