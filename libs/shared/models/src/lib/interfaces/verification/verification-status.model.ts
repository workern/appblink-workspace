/**
 * Verification Status enum
 * Matches the Dart VerificationStatus enum
 */
export enum VerificationStatus {
  PENDING = 'pending',
  VERIFIED = 'verified',
  REJECTED = 'rejected',
  NOT_STARTED = 'not_started'
}
