import * as assert from 'assert';
import * as billingEntitlementModelModule from './billing-entitlement.model';

describe('billing-entitlement.model', () => {
  // TODO: Replace with meaningful assertions
  it('should be importable without errors', () => {
    assert.ok(billingEntitlementModelModule);
  });
});
