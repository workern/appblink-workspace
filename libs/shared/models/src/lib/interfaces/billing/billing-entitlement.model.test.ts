import * as assert from 'assert';
import * as billing-entitlement.modelModule from './billing-entitlement.model';

describe('billing-entitlement.model', () => {
  // TODO: Replace with meaningful assertions
  it('should be importable without errors', () => {
    assert.ok(billing-entitlement.modelModule);
  });
});
