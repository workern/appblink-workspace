import * as assert from 'assert';
import * as purchasable-product.modelModule from './purchasable-product.model';

describe('purchasable-product.model', () => {
  // TODO: Replace with meaningful assertions
  it('should be importable without errors', () => {
    assert.ok(purchasable-product.modelModule);
  });
});
